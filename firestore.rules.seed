rules_version = '2';
// TEMPORARY seed-only rules — replace with production firestore.rules after seeding.
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
